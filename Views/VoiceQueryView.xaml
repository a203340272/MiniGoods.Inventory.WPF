<UserControl x:Class="MiniGoods.Inventory.WPF.Views.VoiceQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MiniGoods.Inventory.WPF.Converters">
    <UserControl.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBool"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource ContentBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 页头 -->
        <Border Grid.Row="0" Background="White"
                BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1"
                Padding="24,16">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🎤" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="语音查询" Style="{StaticResource PageTitle}"/>
                    <TextBlock Text='说出商品名称，例如："可乐多少钱""牙刷价格""方便面现在卖多少"' Style="{StaticResource PageSubtitle}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 操作区 -->
        <Border Grid.Row="1" Background="White"
                BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1"
                Padding="24,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 按钮行 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <!-- 语音按钮 -->
                    <Button Command="{Binding StartVoiceCommand}"
                            IsEnabled="{Binding IsRecognizing, Converter={StaticResource InverseBool}}"
                            Style="{StaticResource PrimaryBtn}"
                            FontSize="14" Padding="20,10" Margin="0,0,12,0">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎙️" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="开始语音查询"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRecognizing}" Value="True">
                                                    <Setter Property="Text" Value="正在识别..."/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <!-- 文字搜索按钮 -->
                    <Button Content="🔍 按文字搜索" Command="{Binding SearchByTextCommand}"
                            Style="{StaticResource NeutralBtn}"
                            FontSize="14" Padding="16,10"/>
                </StackPanel>

                <!-- 输入/识别结果框 -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="识别/搜索词：" FontSize="13" Foreground="#4A5568"
                               VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding RecognizedText, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="False"
                             FontSize="14" Padding="10,8"
                             VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRecognizing}" Value="True">
                                        <Setter Property="Background"  Value="#EBF3FD"/>
                                        <Setter Property="IsReadOnly"  Value="True"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Grid>
        </Border>

        <!-- 结果列表 -->
        <Border Grid.Row="2" Background="White" Margin="20,16,20,20"
                BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Padding="12,8"
                        BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="匹配结果" FontSize="13" FontWeight="SemiBold" Foreground="#2D3748"/>
                        <TextBlock FontSize="12" Foreground="#7A8FA6" VerticalAlignment="Center" Margin="8,0,0,0">
                            <Run Text="共"/>
                            <Run Text="{Binding MatchedProducts.Count, Mode=OneWay}"/>
                            <Run Text="条"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding MatchedProducts}"
                          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                          Margin="1,0,1,1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"      Binding="{Binding ProductID}" Width="50"/>
                        <DataGridTextColumn Header="商品名称" Binding="{Binding Name}"      Width="*"/>
                        <DataGridTextColumn Header="分类"    Binding="{Binding Category}"  Width="80"/>
                        <DataGridTextColumn Header="条码"    Binding="{Binding Barcode}"   Width="120"/>
                        <DataGridTextColumn Header="单价(¥)"  Binding="{Binding Price, StringFormat={}{0:F2}}" Width="100"/>
                        <DataGridTextColumn Header="库存"    Binding="{Binding Stock}"     Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
